import React from 'react';
import { Selected } from '../../../widgets/ui/Selected';

const CompletedTodos: React.FC = () => {
    return (
        <div>
            <Selected />
        </div>
    );
};

export default CompletedTodos;